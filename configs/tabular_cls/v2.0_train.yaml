inherits: configs/tabular_cls/base.yaml

project:
  group: v2.0-train
  tags: [tabular, classification, titanic, train, mlp, v2.0]

runtime:
  version: v2.0
  lineage: scratch
  parent: v1.0

data:
  dataset: titanic
  data_file: data/titanic.csv
  val_split: 0.2
  batch_size: 32

model:
  hidden_dim: 64
  dropout: 0.3

training:
  epochs: 50
  lr: 0.0005
  weight_decay: 0.001
  max_train_batches: null
  max_val_batches: null
  checkpoint_name: v2.0_train_best.pt
