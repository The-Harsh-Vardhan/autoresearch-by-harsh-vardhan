inherits: configs/tabular_cls/base.yaml

project:
  group: v2.0-control
  tags: [tabular, classification, titanic, control, logistic, v2.0]

runtime:
  version: v2.0
  lineage: scratch
  parent: v1.0

data:
  dataset: titanic
  data_file: data/titanic.csv
  val_split: 0.2
  batch_size: 32

model:
  kind: logistic

training:
  epochs: 1
  max_train_batches: 0
  checkpoint_name: v2.0_control.pt
